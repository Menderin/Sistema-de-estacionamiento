{
  "scripts": {
    "build": "tailwindcss -i ./assets/css/styles.css -o ./assets/css/output.css && npm run build-dist",
    "build-dist": "powershell -Command \"Remove-Item -Recurse -Force dist -ErrorAction SilentlyContinue; New-Item -ItemType Directory -Path dist; Copy-Item index.html, login.js, sectores.js dist/; Copy-Item -Recurse pages, assets, scripts dist/\"",
    "watch": "tailwindcss -i ./assets/css/styles.css -o ./assets/css/output.css --watch"
  },
  "devDependencies": {
    "tailwindcss": "^3.4.19"
  },
  "dependencies": {
    "@capacitor/android": "^8.4.0",
    "@capacitor/camera": "^8.2.0",
    "@capacitor/cli": "^8.4.0",
    "@capacitor/core": "^8.4.0",
    "@capacitor/local-notifications": "^8.2.0"
  }
}
